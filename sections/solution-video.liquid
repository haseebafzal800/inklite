{% if section.settings.enable_video %}
<div class="video-container">
    <div class="container">
        <div class="iframe-wrapper">
        {% if section.settings.video_iframe != blank %}
        	{{section.settings.video_iframe}}
        {% else %}
          <iframe width="100%" height="500px" src="https://www.youtube.com/embed/ttNl9364UMg"
                  title="YouTube video player" frameborder="0" 
                  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                  allowfullscreen></iframe>
        {% endif %}
        </div>
    </div>
</div>
{% endif %}

{% schema %}
{
  "name": "Solution Video",
  "settings": [
  	{
      "type": "header",
      "content": "Featured Products"
    },
    {
      "type": "checkbox",
      "id": "enable_video",
      "label": "Enable Video?",
      "default": true
    },
    {
      "type": "html",
      "id": "video_iframe",
      "label": "Video Code",
      "info": "Paste embed code for vidoe"
    }
  ],
  "presets": [
    {
      "name": "Solution Video",
       "blocks": [

      ]
    }
  ]
}
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}